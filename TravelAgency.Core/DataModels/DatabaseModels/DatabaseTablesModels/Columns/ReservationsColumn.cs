namespace TravelAgency.Core
{
    /// <summary>
    /// Reservations column
    /// </summary>
    public enum ReservationsColumn
    {
        client_id,
        client,
        trip_id,
        booked_places,
        name,
        reservation_date,
        expiration_date
    }
}
